# 1.2.2 需求执行状态与交接(A–G)

> 目标版本:`1.2.2`(开发版;正式版需用户指示后发布)
> 约束:零系统配置写入、零主题文件写入、全部官方扩展点;老数据迁移必须**幂等**、只操作插件自身数据。
> 最近一次提交见本文件所在提交;每项完成后在下方更新状态与验证方式。

## 状态总览

| 项 | 内容 | 状态 | 说明 |
|---|---|---|---|
| **A** | 车辆级「保险公司」+ 保险项精简 + 循环间隔(交强险/商业险/车船税=每年,可"不循环")+ 老数据幂等迁移 | 后端 ✅ / 迁移 ✅ / 控制台类型 ✅ / **表单改造待做** | `Car.spec.insurer`、`Reminder.repeatMonths` 已就绪;`migrateCarInsurers()` 已在插件启动时执行(提升 + 去重) |
| **B** | 年检按「首次登记日期 + 车型规则」自动推算(第 2/4 年免检申领 → 第 6/10 年上线 → 第 11 年起每年)+ 手动覆盖 + 注明依据 | 规则 ✅ / 推算接入 ✅ / **表单展示与手动覆盖待做** | `VehicleSupport.nextInspection()`;finder 中年检未填日期时自动推算,阶段写入标题(如"年检·免检申领")、依据写入 `CarEventVo.ruleNote` |
| **C** | 年检 / 车船税默认「与保险同期」(可取消) | ⏳ 待做 | 前置(类型/模型)已就绪;需表单联动 + 保存时同步日期 |
| **D** | 同车同日多事项提醒合并(后台列表仍逐项) | ✅ | `ImportantDateRouter` 按 `carName + dateText` 分组合并,标题形如 `小鹏 · 交强险 / 商业险 / 年检`;日期不同则不合并 |
| **E** | 第 6 年进入上线检验期提示 + 自动切换为每年规则 | ⏳ 待做 | 规则层已能算出阶段(`phase`);需在提醒/界面给出一次性提示 |
| **F** | 年检提前 30 天 + 办理时长提示 | ✅ | 表单提示:"年检默认提前 30 天提醒(办理含上线检验通常需 2–3 个工作日)";`REMINDER_PRESETS` 年检默认 30 天 |
| **G** | 仪表盘小分页(默认每页 5 条、可配可关闭),口径与前台一致 | 设置 ✅ / 下发 ✅ / **小组件 UI 待做** | 设置项 `dashboardPageSize`(默认 5)、`dashboardPagination`(默认开);`/important-dates-reminders` 已下发;仍需 `ReminderWidget.vue` 实现分页并改为显示完整提醒 |

## 后续实现要点(按顺序)

1. **G UI — `console/src/components/ReminderWidget.vue`**
   - 读取 `dashboardPageSize` / `dashboardPagination`;
   - 底部小分页:`‹ 1 / N ›` + "共 X 条";翻页仅切本地状态(数据一次拉全,不重新请求);
   - 数据变化导致当前页越界时回退到最后一页;
   - 显示**完整提醒**(不再受"每类 2 条"限制);分页样式对齐现有日志弹窗分页。

2. **A 表单 — `console/src/components/CarFormModal.vue`**
   - 「基本信息 / 证照」段新增**车辆级「保险公司」**输入(常用公司快捷提示);
   - 到期项行精简为:**险种 / 保单号 / 到期日 / 提前提醒天数 / 循环间隔 / 启用**;
   - 循环间隔下拉:`每年(12)` / `每两年(24)` / `每三年(36)` / `不循环(0)` / `自定义月数`;
   - 保险项提供"**使用其他保险公司**"可选覆盖(展开才填,默认隐藏)。

3. **B 表单展示** — 年检行:未手填日期时显示"按规则自动推算:2026-10-31(免检申领)"+ 依据文案(`ruleNote`),并提供「手动指定日期」开关;缺少首次登记日期时提示补填。

4. **C 同期联动** — 年检 / 车船税行加「与保险同期」勾选(默认开):勾选时日期随交强险到期日联动(只读显示),取消后可独立填写;保存时写入相同日期。

5. **E 提示** — 当车辆按规则进入第 6 年(上线检验期)时,在后台横幅/提醒中给出一次性提示:"「小鹏」已进入上线检验期(每年一次),年检规则已切换为每年"。

6. **收尾** — 全新 Halo 2.26 实例:回归 `halo-smoke.mjs`(**44/44**)+ 浏览器端到端(座驾表单、年检推算展示、同期联动、仪表盘分页、提醒合并)→ 生成 **`1.2.2`** 开发版交付;按 `docs/releasing.md` 规范,用户指示后再发正式版。

## 验证基线(已完成部分)

- `gradlew compileJava` 通过(A/B/D/F/G 数据通路共 5 次提交);
- 控制台 `npm run build` 通过(F、类型补齐);
- 尚未跑整轮回归 —— **1.2.2 交付前必须**在全新 2.26 实例跑满 44/44 并做浏览器验证。
