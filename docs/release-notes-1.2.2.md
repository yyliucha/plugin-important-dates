## v1.2.2 正式版 · 修复附件库「分类 / 策略」来源范围

### ⚠️ 重要提示（请先读）

> **本版本功能测试尚不完整，可能存在导致数据异常甚至数据丢失的严重问题（毁灭性 Bug）。**
> 请**务必先完整备份站点数据（数据库 + 附件）**，并**谨慎升级**；建议先在测试站点验证无误后再用于生产环境。
>
> 说明：本版的实际测试范围是「全新 Halo 2.26 实例自动化回归 **50/50 PASS** + 浏览器端到端 **13/13 PASS** + 附件库来源范围专项 **5/5 PASS**」，**尚未覆盖**长期运行、多主题兼容、极端数据量等场景；开发版（`1.2.2-SNAPSHOT`）已先行发布供体验，本次为**正式版 `1.2.2`**，功能与开发版一致，后续会持续修复与完善，欢迎反馈问题。
>
> **升级后请强刷一次浏览器（Ctrl+Shift+R / Cmd+Shift+R）**：Halo 控制台会缓存插件的控制台包，不强刷时可能看不到新界面（如侧边栏没有「记得」、仪表盘小组件消失）。

### 修复：附件库「分类 / 策略」范围不再显示空白

- **问题**：座驾相册与人员大头贴的「从附件库选择」原先要求 **分类 + 策略同时命中**。站点里只要有一项与实际数据不一致（图片没有分组、或用了别的存储策略），列表就会是空的——人员大头贴弹窗甚至只显示一个空白框，看起来像"设置没生效"。
- **现在**：优先按「分类 + 策略」过滤；**命中为空时自动放宽**为「仅分类」→「仅策略」→「全部图片」，并在弹窗顶部用一行说明当前生效范围与各自命中数，例如：
  `设置中「分类 + 策略」没有同时命中的图片，已放宽为仅按分类显示 12 张（分类「文章插图」命中 12 张 · 策略「大头贴」命中 0 张）`
- **「未分组」正确匹配**：设置里的「未分组」现在按"没有分类的图片"处理（此前被当成一个真实分组名去比对，必然 0 张）。
- **不再自动选第一个分类 / 策略**：旧行为在未配置时会静默把附件库收窄到某个分类 + 策略（正是"看起来没生效"的来源）；现在未配置就是「不限定」，显示全部图片。已保存但已失效的分类 / 策略（分组被删除等）会自动回到「不限定」。
- **上传归档不变**：仍按设置的分类 / 策略存放；分类为「未分组」时不提交分组名（避免把 `__ungrouped__` 当成真实分组）。
- **界面**：范围说明单独一行，长文案不再挤压弹窗标题；底部仍显示「当前显示 N 张 · 附件库共 M 张图片」，并提供「显示全部图片（忽略分类 / 策略）」开关。

### 验证（全新 Halo 2.26 实例）

- 冒烟回归 `halo-smoke.mjs` **50/50 PASS**；浏览器端到端 `browser-121-e2e.mjs` **13/13 PASS**；
- **附件库来源范围专项 5/5 PASS**（`verify-album-scope.mjs`）：分类命中 + 策略不命中 → 放宽为仅分类并给出命中数（座驾 / 人员两条路径都验）；「未分组」正确过滤；未配置时显示全部；控制台零脚本错误。

### 兼容与升级

- **Halo 2.26 及以上**；由 1.2.0 / 1.2.1 / 1.1.x 升级：停用 → 卸载 → 安装本版 → 启用，**数据不会丢失**；
- 若主题目录存在自定义覆盖文件 `templates/important-dates.html`，请用仓库 `docs/theme-override/` 下最新版替换（或删除该文件使用插件默认模板）。
