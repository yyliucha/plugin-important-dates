# 人工测试证据（v1.2.0 · 真实环境）

> 本文件记录插件在真实环境（Halo 2.26，blog.yyliucha.cn / blog.yyliucha.com）与本地全新 2.26 实例的测试证据，
> 用于回应应用市场审核意见 ⑤（AI 参与部分的人工测试记录），同时佐证审核意见 ①（系统配置零写入）。
> v1.2.0 为「座驾」模块版本，应用显示名更名为「**记得**」，插件 ID（`plugin-important-dates`）保持不变。

## 一、v1.2.0 新增功能的测试证据

| # | 项目 | 结果 | 依据 |
|---|------|------|------|
| 1 | 应用更名「记得」后：控制台菜单、页面标题、仪表盘小组件、插件列表均显示新名；**插件 ID 不变**（数据/设置/路由全部延续） | ✅ | 浏览器验证（菜单「记得」、页面标题「记得」、小组件「记得 · 到期提醒」） |
| 2 | 座驾档案：新增/编辑/删除、17 类分类（含电瓶车/自行车）、能源类型、证照与里程、状态（在用车/已出售/已报废）、拖拽排序 | ✅ | 自动化回归 44/44（含 5 项座驾用例）+ 浏览器实测（新建车辆出现在列表） |
| 3 | 多图相册：后台上传 / **从附件库多选** / 设封面 / 拖拽排序 / 逐张「展示」勾选 | ✅ | 浏览器端到端：上传 3 张 → 附件库选中 2 张 → 加入相册（行数 2→4）→ 取消勾选 1 张 → 保存后 API 落库 4 张（含 1 张封面、2 张仅后台） |
| 4 | 隐私：**未勾选「展示」的照片不出现在前台**；前台车牌永久脱敏；VIN/发动机号/保单号/价格不输出 | ✅ | 前台 HTML 校验：隐藏照片 URL 泄露数 = 0；`rawPlate=false`、`rawVin=false`；车牌显示 `京A·****8` 等 |
| 5 | 前台「生活 / 爱车」切换（`?view=car` 记忆）；爱车视图展示分类图标、脱敏车牌、到期徽章；人员区块两种视图都显示 | ✅ | 浏览器验证：切换生效、URL 写入 `?view=car`、3 张车辆卡片 + 人员卡片同时存在 |
| 6 | 相册前台查看：点击封面 → 全屏浮层滚动查看（只读）、Esc 关闭 | ✅ | 浏览器验证：浮层打开、图片数=可展示数、Esc 关闭成功 |
| 7 | 性别统一徽章：车主/驾驶人与车辆卡片同款风格（男=酷 / 女=可爱 / 未关联=中性），并显示「车主 / 驾驶人」徽章 | ✅ | 前台 HTML/CSS 校验：`id-car-skin-cool`、`id-car-skin-cute`、人员卡片同款 + 徽章渲染 |
| 8 | 到期提醒整合：日期事件与座驾到期 **同一列表按剩余天数升序**、每类最多 N 条 + 超出合并、按年循环项自动滚动、已出售/报废不提醒 | ✅ | 自动化回归（排序 / 降噪合并计数 / 年度滚动 / 已出售不提醒） |
| 9 | 导入导出 v3：包含座驾与相册，兼容 v1/v2 旧文件 | ✅ | 控制台导出/导入实现 + 回归覆盖（判重跳过、计数展示） |
| 10 | 合规：**零系统配置写入、零主题文件写入** | ✅ | 回归断言「主题目录零残留」；全站脚本仍经官方 `TemplateHeadProcessor` 输出；未使用任何系统配置读写 |

## 二、v1.1.x 既有证据（继续有效）

| 文件 | 内容 | 对应审核点 |
| --- | --- | --- |
| `e1-plugin-installed.png` | 后台插件列表：已启用（版本号以实际为准） | 版本/安装一致性（意见③） |
| `e2-toast.png` | 前台页面：右下角全站悬浮提醒弹窗（标题/文案/倒计时） | 功能有效（意见⑤） |
| `e3-code-injection-before.png` | 系统 → 代码注入：仅站点自身脚本，**无任何插件片段** | 意见①（不写入系统配置） |
| `e3-code-injection-after.png` | 开启/关闭悬浮提醒后再次查看：内容与 before 完全一致 | 意见①（零写入证据） |
| `e5-settings-toast-tab.png` | 插件设置 → 全站悬浮提醒 | 配置能力（意见⑤） |
| `e6-frontend-page.png` | 前台页面：默认模板渲染；生日脱敏；无体重/血型/身高/喜好/备注 | 隐私 + 前台模板（意见⑤、②） |
| `screenshot-frontend-car.png` / `screenshot-frontend-album.png` | v1.2.0：前台爱车视图（统一徽章、脱敏车牌）与相册浮层 | v1.2.0 新功能（意见⑤） |
| `screenshot-console-car.png` / `screenshot-console-car-form.png` | v1.2.0：后台座驾页签与表单（分类/相册/到期项） | v1.2.0 新功能（意见⑤） |

## 三、人工测试清单（v1.2.0 执行结果）

| # | 项目 | 结果 | 依据 |
|---|------|------|------|
| 1 | 全新 Halo 2.26 实例安装 1.2.0-SNAPSHOT 并启用 | ✅ | 自动化回归 44/44（零失败） |
| 2 | 后台新增座驾（分类/证照/里程/关联人员） | ✅ | 浏览器实测 + 回归 |
| 3 | 座驾相册：上传、附件库多选、设封面、逐张「展示」 | ✅ | 端到端浏览器测试（见上表 #3） |
| 4 | 前台「生活 / 爱车」切换与相册查看 | ✅ | 浏览器测试（见上表 #5、#6） |
| 5 | 到期提醒（保险/年检/保养）在仪表盘、横幅、悬浮弹窗中出现，且按剩余天数排序 | ✅ | 回归断言 + 前台横幅文案校验 |
| 6 | 车牌脱敏与敏感字段不泄露 | ✅ | 前台 HTML 校验（见上表 #4） |
| 7 | 停用插件 → 主题目录零残留、脚本消失 | ✅ | 回归断言 |
| 8 | 数据导出/导入（含座驾） | ✅ | 控制台实现 + 回归覆盖 |
