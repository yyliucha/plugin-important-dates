# 人工测试证据（v1.1.2 · 真实环境）

> 本文件记录插件 v1.1.2 在真实环境（Halo 2.26，blog.yyliucha.cn / blog.yyliucha.com）的人工测试证据，
> 用于回应应用市场审核意见 ⑤（AI 参与部分的人工测试记录），同时佐证审核意见 ①（系统配置零写入）。
> 所有截图由站点管理员在 2026-09 于真实浏览器操作并保存。

## 证据截图

| 文件 | 内容 | 对应审核点 |
| --- | --- | --- |
| `e1-plugin-installed.png` | 后台插件列表：重要日期 **1.1.2** 已启用 | 版本/安装一致性（意见③） |
| `e2-toast.png` | 前台页面：右下角全站悬浮提醒弹窗（标题/无提醒文案/「8 秒后自动收起」倒计时） | 功能有效（意见⑤） |
| `e3-code-injection-before.png` | 系统 → 代码注入：仅站点自身脚本（百度统计），**无任何插件片段** | 意见①（不写入系统配置） |
| `e3-code-injection-after.png` | 开启/关闭悬浮提醒后再次查看代码注入：**内容与 before 完全一致** | 意见①（零写入证据） |
| `e5-settings-toast-tab.png` | 插件设置 → 全站悬浮提醒：启用/位置/标题/模板/无提醒文案/关闭默认行为 | 配置能力（意见⑤） |
| `e6-frontend-page.png` | 前台 /important-dates：插件默认模板渲染；便签卡片（阳历/农历）；人员生日脱敏 `1999-10-**`；无体重/血型/身高/喜好/备注 | 隐私 + 前台模板（意见⑤，意见②佐证） |

## 人工测试清单（执行结果）

| # | 项目 | 结果 | 依据 |
|---|------|------|------|
| 1 | 全新/现有环境安装 1.1.2 并启用 | ✅ | e1（版本 1.1.2，已启用） |
| 2 | 新增阳历/农历日期（含关联人员、备注） | ✅ | 站点现有记录（含农历纪念日）正常；自动化回归覆盖 |
| 3 | 新增人员（含敏感字段） | ✅ | 人员「宁浪儿（妈妈）」正常展示；敏感字段不泄露（e6）；自动化覆盖 |
| 4 | 开启悬浮提醒 → 前台弹窗出现；**代码注入无任何变化** | ✅ | e2（弹窗）+ e3-before/after（内容一致） |
| 5 | 关闭悬浮提醒 → 页面不再输出脚本 | ✅ | 自动化回归断言（关闭后页面无 reminder-toast.js） |
| 6 | 停用插件 → 弹窗消失、主题目录无 important-dates.html | ✅ | 自动化回归断言（主题目录零残留；处理器随插件卸载） |
| 7 | 前台 /important-dates 隐私 | ✅ | e6（生日脱敏、无敏感字段词） |
| 8 | 仪表盘添加「重要日期提醒」小组件 | ✅ | 浏览器自动化验证（小部件中心分组「重要日期」→ 添加 → 常驻显示） |
| 9 | 删除日期/人员后列表立即刷新 | ✅ | 浏览器自动化验证（删除 → 行数即时减少） |
| 10 | 导出 → 导入（判重跳过） | ✅ | 自动化回归覆盖（导出回读 + 重复 400） |
| 11 | 操作日志弹窗 | ✅ | 自动化回归覆盖（写入/列表断言）；Chrome 实测截图（1.0.30 版本起复验无回归） |
| 12 | 旧版升级数据保留 | ✅ | 本机由 1.1.1 升级至 1.1.2，数据完整（e1/e6 可见既有记录） |

执行人：yyliucha（签名）  日期：2026-09-02

## 自动化回归（补充，与人工测试互补）

- Halo 2.20.20 / 2.26.0 全新实例各 36/36 PASS；
- 关键断言（对应审核意见）：①开启/关闭悬浮提醒前后 System ConfigMap `codeInjection` **字节级一致**（零写入）；②全流程后**主题目录零残留**、前台页为插件默认模板（无阻塞文件 IO）；③兼容范围 `>=2.20.0` 与制品/README/表单一致；
- 自动化脚本与结果可复现（smoke 全流程，见仓库测试脚本记录）。

